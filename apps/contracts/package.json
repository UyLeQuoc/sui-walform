{
  "name": "@walform/contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "WalForm Move package (Sui testnet) + TypeScript deploy/upgrade/codegen scripts.",
  "scripts": {
    "test": "sui move test",
    "build": "sui move build",
    "publish": "bun run scripts/publish.ts",
    "upgrade": "bun run scripts/upgrade.ts",
    "setup-public-allowlist": "bun run scripts/setup-public-allowlist.ts",
    "codegen": "sui-ts-codegen generate",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'noop — add eslint later'"
  },
  "dependencies": {
    "@mysten/bcs": "^2",
    "@mysten/codegen": "latest",
    "@mysten/sui": "^2",
    "dotenv": "^17"
  },
  "devDependencies": {
    "@types/bun": "^1",
    "@types/node": "^20",
    "typescript": "^5.7.0"
  }
}
