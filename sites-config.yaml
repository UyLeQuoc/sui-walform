contexts:
  testnet:
    # module: site
    # portal: wal.app
    package: 0x22b8c1496650eb45fbcca0f8f37fae77ed33b7d4eaab4da5f0bb9b62a8708dcb
    staking_object: 0xbe46180321c30aab2f8b3501e24048377287fa708018a5b7c2792b35fe339ee3
    general:
       wallet_env: testnet
       walrus_context: testnet # Assumes a Walrus CLI setup with a multi-config containing the "testnet" context.
       walrus_package: 0xa998b8719ca1c0a6dc4e24a859bbb39f5477417f71885fbf2967a6510f699144
       # wallet_address: 0x1234...
       # rpc_url: https://fullnode.testnet.sui.io:443
       # wallet: /path/to/.sui/sui_config/client.yaml
       # walrus_binary: /path/to/walrus
       # walrus_config: /path/to/testnet/client_config.yaml
       # gas_budget: 500000000
  mainnet:
    # module: site
    # portal: wal.app
    package: 0x5a0c509a659ba982f91ff1189872b8d528f8c02b5f6285a3931fc4c2869ccc9c
    staking_object: 0x10b9d30c28448939ce6c4d6c6e0ffce4a7f8a4ada8248bdad09ef8b70e4a3904
    general:
       wallet_env: mainnet
       walrus_context: mainnet # Assumes a Walrus CLI setup with a multi-config containing the "mainnet" context.
       walrus_package: 0x98da433aa0139512c210597b1c5e3df6cd121d8d77f8652691bb66fadfc8aa1b
       # wallet_address: 0x1234...
       # Mysten's public fullnode serves STALE object state (public JSON-RPC is
       # being decommissioned — mainnet week of 2026-07-20, off 2026-07-31), which
       # fails the deploy with "asked version N is higher than the latest M".
       # ZAN reads fresh. See docs/RPC_MIGRATION.md.
       rpc_url: https://api.zan.top/node/v1/sui/mainnet/0dbc5a32c8e64375a953e92166361ee0
       # wallet: /path/to/.sui/sui_config/client.yaml
       # walrus_binary: /path/to/walrus
       # walrus_config: /path/to/mainnet/client_config.yaml
       # site-builder needs ONE coin >= gas_budget to reserve. The default
       # (500000000 = 0.5 SUI) exceeds the deploy wallet's balance now that the
       # same key also sponsors uploads. A real deploy costs ~0.006 SUI, so 0.3
       # SUI is ~50x headroom while still fitting the current balance. Raise it
       # again once the wallet is topped up.
       gas_budget: 300000000

default_context: mainnet
