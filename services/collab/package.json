{
  "name": "@walform/collab",
  "version": "0.0.0",
  "private": true,
  "description": "PartyKit Yjs document server — one room per formId, server-persisted draft (see docs/COLLAB_DESIGN.md). Auto-started by `bun run dev`; deploy with `bun run collab:deploy`.",
  "scripts": {
    "dev": "partykit dev",
    "deploy": "partykit deploy"
  },
  "dependencies": {
    "y-partykit": "latest",
    "yjs": "^13.6.20"
  },
  "devDependencies": {
    "partykit": "latest"
  }
}
